# <%= element.name %>
<%-: element.documentation | markdown %>
<% element.getChildren().forEach(function (elem) {%>
<% if (elem.name) {%>##<%= elem.name %> <%if (elem.multiplicity) { %><%= elem.multiplicity %> <% } %>
<%= elem.documentation %>
<% }}) %>


